apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: {{ .Values.hostname }}
  namespace: {{ .Release.Namespace }}
  annotations:
    nginx.ingress.kubernetes.io/ssl-passthrough: "true"
spec:
  rules:
  - host: {{ required "A valid 'hostname' is required" .Values.hostname }}
    http:
      paths:
      - path: /
        backend:
          serviceName: dex
          servicePort: 443

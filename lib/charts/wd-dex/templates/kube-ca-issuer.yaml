apiVersion: cert-manager.io/v1alpha2
kind: Issuer
metadata:
  name: kube-ca-issuer
spec:
  ca:
    secretName: kube-ca-issuer-cert

# create secret with:
# kubectl -n kube-dex create secret tls --cert /etc/kubernetes/pki/ca.crt --key /etc/kubernetes/pki/ca.key kube-ca-issuer-cert
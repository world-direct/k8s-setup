apiVersion: v2
name: wd-flannel
type: application
description: |
  Helm v3 chart for deployment of flannel to wd clusters
  Taken from version https://raw.githubusercontent.com/coreos/flannel/3f7d3e6c24f641e7ff557ebcea1136fdf4b1b6a1/Documentation/kube-flannel.yml

# This is from master, as release v0.11.0 is very old, and still uses apiVersion "extensions/v1beta1"
# To compare with new versions, open:
# https://github.com/coreos/flannel/compare/3f7d3e6c24f641e7ff557ebcea1136fdf4b1b6a1...master
# Then locate the 'Documentation/kube-flannel.yml' and apply the changes to the templates

# This is the chart version. This version number should be incremented each time you make changes
# to the chart and its templates, including the app version.
version: 0.0.1

# This is the version number of the application being deployed. This version number should be
# incremented each time you make changes to the application.

# lasttag-commithash-release (for reapplying changed configuration)
appVersion: 0.11.0-3f7d3e6-0
